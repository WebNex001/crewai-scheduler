# CrewAI Scheduler 下一代版本路线图

## 当前版本：v3.2.0

---

## v3.3.0 — 稳定性与体验优化（近期）

### 3.3.1 工作流可靠性增强
- [ ] **阶段级超时控制**：为每个阶段配置独立超时时间（如 development 阶段 600s，review 阶段 300s），防止单个阶段卡死导致整个工作流僵死
- [ ] **智能重试策略**：区分临时错误（429/503）和永久错误（401/400），临时错误增加重试次数，永久错误立即失败
- [ ] **API 健康探测**：工作流启动前探测 API 可用性，避免在 API 宕机时浪费阶段执行
- [ ] **断点续传增强**：支持从任意历史 attempt 恢复（而非仅最新一次），保留多版本 checkpoint

### 3.3.2 开发者体验
- [ ] **Web UI 仪表盘**：基于 Streamlit/Gradio 的轻量级 Web 界面，可视化工作流进度、阶段输出、代码文件树
- [ ] **实时日志推送**：WebSocket 或 SSE 推送执行日志到前端，替代轮询 `workflow-status`
- [ ] **项目模板系统**：预置项目模板（Web App、CLI Tool、API Service、Mobile App），创建项目时一键选择
- [ ] **配置文件热重载**：修改 `config.json` 后无需重启进程即可生效

### 3.3.3 代码质量
- [ ] **静态分析集成**：development 阶段完成后自动运行 linter（eslint/pylint/golint），将结果注入 code_review 上下文
- [ ] **自动格式化**：集成 prettier/black/gofmt，写入文件后自动格式化
- [ ] **依赖自动安装**：检测代码中的 import/require，自动生成并执行依赖安装命令
- [ ] **类型检查**：支持 TypeScript/Python 类型检查，将错误反馈到 development 阶段

### 3.3.4 监控与可观测性
- [ ] **Token 用量精细化统计**：按阶段、按项目、按模块统计 Token 消耗，生成成本报告
- [ ] **性能瓶颈分析**：记录每个阶段的 API 等待时间、压缩时间、文件 I/O 时间
- [ ] **告警机制**：工作流失败/崩溃时发送通知（Webhook/邮件/企业微信）

---

## v3.4.0 — 多智能体协作深化（中期）

### 3.4.1 多 Agent 并行协作
- [ ] **评审会议模式**：code_review 阶段引入多 Agent 辩论（架构师 + 安全专家 + 性能专家各自评审，然后综合意见）
- [ ] **结对编程模式**：development 阶段双 Agent 协作（一个写代码，一个实时 review）
- [ ] **专家咨询模式**：特定阶段可召唤领域专家 Agent（如数据库专家优化 Schema、前端专家优化 UI）

### 3.4.2 动态工作流
- [ ] **条件分支**：根据项目类型/规模动态调整阶段（如小型项目跳过 architecture，直接 design → dev）
- [ ] **循环迭代**：支持自定义循环（如 TDD 模式：dev → test → dev → test 循环直到通过）
- [ ] **子工作流**：大模块可触发子工作流（独立的 8 阶段流水线），完成后合并回主工作流

### 3.4.3 知识库与记忆
- [ ] **项目知识库**：每个项目维护独立知识库（技术决策、踩坑记录、API 文档），后续阶段自动检索
- [ ] **跨项目学习**：自动提取可复用组件/工具函数，建立组织级代码库
- [ ] **RAG 增强**：集成向量数据库，将历史项目文档、最佳实践作为 RAG 上下文注入

---

## v4.0.0 — 自主 AI 团队（远期）

### 4.0.1 完全自主执行
- [ ] **自调度系统**：AI 自主决定项目优先级、资源分配、阶段调度，无需人工触发 `execute-workflow`
- [ ] **需求自动细化**：用户只需输入一句话需求，AI 自动分解为完整的需求规格说明书
- [ ] **自动测试执行**：testing 阶段生成的测试代码自动运行，将实际测试结果反馈给 AI
- [ ] **自动部署验证**：deployment 阶段自动构建 Docker 镜像并部署到测试环境，验证可用性

### 4.0.2 多模态支持
- [ ] **UI 设计生成**：detailed_design 阶段生成 UI 原型图（通过多模态模型）
- [ ] **架构图生成**：architecture 阶段生成 C4 模型图、时序图（Mermaid/PlantUML）
- [ ] **代码可视化**：自动生成代码调用关系图、依赖关系图

### 4.0.3 企业级特性
- [ ] **多租户支持**：隔离不同团队/用户的数据和配置
- [ ] **权限控制**：RBAC 角色权限（管理员/项目经理/开发者/观察者）
- [ ] **审计日志**：完整记录谁、何时、做了什么操作
- [ ] **CI/CD 集成**：与 GitHub Actions/GitLab CI/Jenkins 集成，工作流完成后自动提交 PR
- [ ] **代码仓库集成**：直接推送代码到 GitHub/GitLab/Gitee，自动生成 commit message 和 PR 描述

### 4.0.4 生态扩展
- [ ] **Plugin 系统**：支持第三方扩展（新的阶段类型、新的代码提取器、新的部署目标）
- [ ] **Marketplace**：社区共享的项目模板、Agent Prompt、工作流配置
- [ ] **API Server**：提供 RESTful API，支持外部系统调用

---

## 技术债务与重构项

### 高优先级
- [ ] **测试覆盖**：当前测试极少，需补充单元测试（pytest）和集成测试
- [ ] **类型注解补全**：部分函数缺少类型注解，影响 IDE 提示和静态检查
- [ ] **配置 Schema 校验**：使用 Pydantic 替代手动 `_validate_config()`

### 中优先级
- [ ] **异步化**：将同步 API 调用改为异步（asyncio/aiohttp），提升并发能力
- [ ] **配置分层**：支持全局配置 + 部门配置 + 项目配置三级覆盖
- [ ] **日志结构化**：统一使用 JSON 结构化日志，便于日志收集和分析

### 低优先级
- [ ] **国际化**：支持英文/中文界面切换
- [ ] **文档自动生成**：从代码注释自动生成 API 文档（pdoc/mkdocs）

---

## 版本发布时间线（预估）

| 版本 | 目标时间 | 核心主题 |
|------|---------|---------|
| v3.3.0 | 2026 Q2 | 稳定性、Web UI、代码质量 |
| v3.4.0 | 2026 Q3 | 多 Agent 协作、动态工作流、RAG |
| v4.0.0 | 2026 Q4~2027 Q1 | 完全自主、多模态、企业级、生态 |

---

## 参与贡献

欢迎通过以下方式参与：
1. 提交 Issue 报告 bug 或建议
2. 提交 PR 实现 roadmap 中的功能
3. 分享使用经验和最佳实践
4. 贡献项目模板和 Agent Prompt
